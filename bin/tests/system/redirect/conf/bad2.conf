zone "." {
	type hint;
	file "hint.db";
};

zone "." {
	type redirect;
	file "redirect.db";
	allow-query { 10.0.1.0; };
/* option 'also-notify' is not allowed in 'redirect' zone '.' */
	also-notify { 1.2.3.4; };
};
