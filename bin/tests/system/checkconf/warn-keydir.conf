/*
 * key-directory defined but doesn't exist.
 */
options {
	directory ".";
};

key-store "test" {
	directory "test.keystoredir";
};

zone dummy {
	type primary;
	file "xxxx";
	key-directory "test.keydir";
};
