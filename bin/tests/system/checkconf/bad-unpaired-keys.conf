dnssec-policy unpaired-keys {
	keys {
		/* zsk without ksk */
		zsk lifetime 30d algorithm 13;
		/* ksk without zsk */
		ksk lifetime 30d algorithm 7;
	};
};

zone "example" {
	type primary;
	file "example.db";
	dnssec-policy unpaired-keys;
};
