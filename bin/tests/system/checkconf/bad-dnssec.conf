zone not-inline {
	type secondary;
	primaries { 127.0.0.1; };
	inline-signing no;
	dnssec-loadkeys-interval 10;

};

zone inline {
	type secondary;
	primaries { 127.0.0.1; };
	inline-signing yes;
	dnssec-loadkeys-interval 10;
};
