/*
 * inline-signing not allowed at view level.
 */
view "a" {
	inline-signing yes;

	zone "." {
		type primary;
		file "root.db.signed";
	};
};

view "b" {
	zone "." {
		type primary;
		file "root.db";
	};
};
