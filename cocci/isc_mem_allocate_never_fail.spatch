@deletion@
type T;
statement S1, S2;
expression V, E1, E2;
@@
(V =
(isc_mem_allocate
|(T *)isc_mem_allocate
)(...);
-if (V == NULL) S1
|V = isc_mem_allocate(...);
-if (V == NULL) S1 else { S2 }
+S2
|if (V == NULL) V = isc_mem_allocate(...);
-if (V == NULL) S1
|V =
-    (T)
     isc_mem_allocate(E1, E2);
)
